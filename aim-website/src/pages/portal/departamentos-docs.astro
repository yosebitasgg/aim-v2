---
import PortalLayoutSidebar from '../../layouts/PortalLayoutSidebar.astro';
---

<PortalLayoutSidebar title="Documentación Departamentos - AIM">
  <div class="p-6 max-w-4xl mx-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-4">Gestión de Departamentos</h1>
      <p class="text-lg text-gray-600">Guía completa para el manejo de departamentos en AIM</p>
    </div>

    <!-- Estado Actual -->
    <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-8">
      <h2 class="text-xl font-semibold text-green-900 mb-4">✅ Estado Actual</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <h3 class="font-medium text-green-800 mb-2">Implementado</h3>
          <ul class="text-sm text-green-700 space-y-1">
            <li>• Campo department en base de datos</li>
            <li>• 7 departamentos predefinidos</li>
            <li>• Integración en backend completa</li>
            <li>• Endpoint GET /api/permissions/departments</li>
            <li>• Hook useDepartments()</li>
            <li>• Componentes DepartmentSelector</li>
          </ul>
        </div>
        <div>
          <h3 class="font-medium text-green-800 mb-2">Departamentos Disponibles</h3>
          <ul class="text-sm text-green-700 space-y-1">
            <li>• Dirección</li>
            <li>• Ventas</li>
            <li>• Operaciones</li>
            <li>• Soporte</li>
            <li>• Finanzas</li>
            <li>• RRHH</li>
            <li>• Marketing</li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Uso del Hook -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Hook useDepartments()</h2>
      <pre class="bg-gray-50 p-4 rounded-lg text-sm overflow-x-auto mb-4"><code>import { useDepartments } from '../hooks/useDepartments';

export function MyComponent() {
  const { 
    departments, 
    loading, 
    error, 
    getDepartmentLabel, 
    getDepartmentOptions 
  } = useDepartments();

  if (loading) return <div>Cargando...</div>;
  if (error) return <div>Error: {error}</div>;

  return (
    <div>
      {departments.map(dept => (
        <div key={dept.value}>
          {dept.label} ({dept.value})
        </div>
      ))}
    </div>
  );
}</code></pre>
    </div>

    <!-- Componentes Disponibles -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Componentes Disponibles</h2>
      
      <div class="space-y-6">
        <div>
          <h3 class="font-medium text-gray-800 mb-2">DepartmentSelector</h3>
          <p class="text-sm text-gray-600 mb-3">Selector principal para formularios</p>
          <pre class="bg-gray-50 p-4 rounded-lg text-sm overflow-x-auto"><code>import DepartmentSelector from '../components/DepartmentSelector';

&lt;DepartmentSelector
  value={selectedDepartment}
  onChange={setSelectedDepartment}
  placeholder="Selecciona un departamento"
  required={true}
  disabled={false}
/&gt;</code></pre>
        </div>

        <div>
          <h3 class="font-medium text-gray-800 mb-2">DepartmentFilter</h3>
          <p class="text-sm text-gray-600 mb-3">Filtro con opción "Todos"</p>
          <pre class="bg-gray-50 p-4 rounded-lg text-sm overflow-x-auto"><code>import { DepartmentFilter } from '../components/DepartmentSelector';

&lt;DepartmentFilter
  value={filter}
  onChange={setFilter}
  showAll={true}
/&gt;</code></pre>
        </div>

        <div>
          <h3 class="font-medium text-gray-800 mb-2">DepartmentBadge</h3>
          <p class="text-sm text-gray-600 mb-3">Badge con colores por departamento</p>
          <pre class="bg-gray-50 p-4 rounded-lg text-sm overflow-x-auto"><code>import { DepartmentBadge } from '../components/DepartmentSelector';

&lt;DepartmentBadge 
  department="ventas" 
  className="custom-class"
/&gt;</code></pre>
        </div>
      </div>
    </div>

    <!-- API Endpoints -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">API Endpoints</h2>
      
      <div class="space-y-4">
        <div class="border-l-4 border-green-500 pl-4">
          <h3 class="font-medium text-gray-800">GET /api/permissions/departments</h3>
          <p class="text-sm text-gray-600">Obtiene lista de departamentos disponibles</p>
          <pre class="bg-gray-50 p-3 rounded text-sm mt-2"><code>// Respuesta
{
  "success": true,
  "data": [
    {
      "key": "DIRECCION",
      "value": "direccion",
      "label": "Dirección"
    },
    // ... más departamentos
  ]
}</code></pre>
        </div>

        <div class="border-l-4 border-blue-500 pl-4">
          <h3 class="font-medium text-gray-800">Filtrado por Departamento</h3>
          <p class="text-sm text-gray-600">Los endpoints de usuarios soportan filtro por departamento</p>
          <pre class="bg-gray-50 p-3 rounded text-sm mt-2"><code>// Ejemplo
GET /api/permissions/users?department=ventas</code></pre>
        </div>
      </div>
    </div>

    <!-- Colores por Departamento -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Colores por Departamento</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="space-y-2">
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-purple-100 text-purple-800">
              Dirección
            </span>
            <code class="text-sm">bg-purple-100 text-purple-800</code>
          </div>
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
              Ventas
            </span>
            <code class="text-sm">bg-green-100 text-green-800</code>
          </div>
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
              Operaciones
            </span>
            <code class="text-sm">bg-blue-100 text-blue-800</code>
          </div>
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
              Soporte
            </span>
            <code class="text-sm">bg-yellow-100 text-yellow-800</code>
          </div>
        </div>
        <div class="space-y-2">
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
              Finanzas
            </span>
            <code class="text-sm">bg-red-100 text-red-800</code>
          </div>
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-pink-100 text-pink-800">
              RRHH
            </span>
            <code class="text-sm">bg-pink-100 text-pink-800</code>
          </div>
          <div class="flex items-center space-x-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-orange-100 text-orange-800">
              Marketing
            </span>
            <code class="text-sm">bg-orange-100 text-orange-800</code>
          </div>
        </div>
      </div>
    </div>

    <!-- Próximos Pasos -->
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6">
      <h2 class="text-xl font-semibold text-blue-900 mb-4">🚀 Próximos Pasos</h2>
      
      <div class="space-y-4">
        <div>
          <h3 class="font-medium text-blue-800 mb-2">Implementación Inmediata</h3>
          <ul class="text-sm text-blue-700 space-y-1">
            <li>• Integrar DepartmentSelector en formularios de usuario</li>
            <li>• Agregar filtros por departamento en tablas</li>
            <li>• Mostrar badges de departamento en listas</li>
            <li>• Probar endpoint GET /api/permissions/departments</li>
          </ul>
        </div>
        
        <div>
          <h3 class="font-medium text-blue-800 mb-2">Mejoras Futuras (Opcional)</h3>
          <ul class="text-sm text-blue-700 space-y-1">
            <li>• CRUD completo de departamentos</li>
            <li>• Jerarquías de departamentos</li>
            <li>• Departamentos por cliente</li>
            <li>• Estadísticas por departamento</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</PortalLayoutSidebar> 