---
// src/pages/casos-de-exito/index.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import { caseStudies } from '../../data/caseStudies.js';
---

<BaseLayout
  title="Casos de Éxito"
  description="Descubre cómo hemos ayudado a empresas líderes a transformar sus operaciones, reducir costos y aumentar la eficiencia con Agentes Inteligentes."
>
  <!-- Hero Section -->
  <div class="bg-gradient-to-b from-teal-50 to-white py-20 text-center">
    <div class="container mx-auto px-4">
      <h1 class="text-4xl md:text-5xl font-black text-teal-800 tracking-tight">
        Resultados Reales. ROI Comprobado.
      </h1>
      <p class="mt-6 text-lg text-teal-600 max-w-2xl mx-auto">
        Estas son las historias de cómo hemos convertido desafíos complejos en ventajas competitivas para nuestros clientes.
      </p>
    </div>
  </div>

  <!-- Case Studies Grid -->
  <div class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="space-y-16">
        {caseStudies.map((study, index) => (
          <div class={`grid grid-cols-1 lg:grid-cols-2 gap-12 items-center ${index % 2 === 1 ? 'lg:grid-flow-dense' : ''}`}>
            <!-- Image -->
            <div class={`${index % 2 === 1 ? 'lg:col-start-2' : ''}`}>
              <div class="rounded-2xl overflow-hidden shadow-lg group">
                <img 
                  src={study.thumbnail} 
                  alt={`Caso de éxito de ${study.client}`} 
                  class="w-full h-80 object-cover group-hover:scale-105 transition-transform duration-500"
                  loading="lazy"
                  onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
                />
                <div class="w-full h-80 bg-teal-100 flex items-center justify-center text-teal-600 font-semibold" style="display: none;">
                  Imagen de {study.industry}
                </div>
              </div>
            </div>
            
            <!-- Content -->
            <div class={`${index % 2 === 1 ? 'lg:col-start-1 lg:row-start-1' : ''}`}>
              <div class="inline-block px-3 py-1 bg-teal-100 text-teal-700 text-sm font-semibold rounded-full mb-4">
                {study.industry}
              </div>
              
              <h2 class="text-3xl md:text-4xl font-bold text-teal-800 mb-6 leading-tight">
                {study.headline}
              </h2>
              
              <p class="text-teal-600 text-lg mb-8 leading-relaxed">
                {study.challenge.substring(0, 200)}...
              </p>

              <!-- Results Preview -->
              <div class="grid grid-cols-2 gap-4 mb-8">
                {study.results.slice(0, 2).map(result => (
                  <div class="bg-teal-50 p-4 rounded-lg border border-teal-100">
                    <div class="text-2xl font-bold text-teal-600">{result.metric}</div>
                    <div class="text-sm text-teal-700">{result.description}</div>
                  </div>
                ))}
              </div>
              
              <a 
                href={`/casos-de-exito/${study.slug}`} 
                class="inline-flex items-center font-bold text-teal-600 hover:text-teal-700 group transition-colors"
              >
                Leer Caso Completo
                <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- CTA Section -->
  <div class="bg-teal-600 py-16">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
        ¿Listo para escribir tu historia de éxito?
      </h2>
      <p class="text-xl text-teal-100 mb-8 max-w-2xl mx-auto">
        Descubre cómo podemos transformar tus procesos y generar resultados similares en tu empresa.
      </p>
      <a 
        href="/contacto" 
        class="inline-block bg-white text-teal-600 font-bold py-4 px-8 rounded-lg hover:bg-teal-50 transition-colors shadow-lg"
      >
        Solicitar Consulta Gratuita
      </a>
    </div>
  </div>
</BaseLayout> 